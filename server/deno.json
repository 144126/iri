{
	"tasks": {
		"dev": "mkdir -p ../dev-logs && deno run --watch --allow-net --allow-env index.ts 2>&1 | tee ../dev-logs/server.log",
		"start": "deno run --allow-net --allow-env index.ts",
		"test": "deno test --allow-net --allow-env index.spec.ts"
	}
}
